package space_habit_frontier.console;

// import java.math.BigDecimal;
// import java.sql.DriverManager;
import java.sql.SQLException;
// import org.jooq.impl.DSL;
// import org.jooq.SQLDialect;
// import space_habit_frontier.console.tables.tables.Users;;

public class App {
	
	public static void main(String[] args) throws SQLException {
		// var conn = DriverManager.getConnection(
		// 	"jdbc:postgresql://localhost:5432/space_habit_frontier_db",
		// 	"shf_owner",
		// 	"ownerpass"
		// );
		// var context = DSL.using(conn, SQLDialect.POSTGRES);
		// var user = context.newRecord(Users.USERS);
		// user.setUsername("test");
		// user.setHashedpw(new byte[] { 1, 2, 3, 4, 5, 6, 7, 8});
		// user.setEmail("Test@mail.com");
		// user.setCreationtimestamp(new BigDecimal(1749405463));
		// user.store();
		System.out.println("Yarrlow");
	}
}
